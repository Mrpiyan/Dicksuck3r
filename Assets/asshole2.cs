using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class asshole2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
    Debug.Log("asdf");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
